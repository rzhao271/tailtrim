language: python
os: linux
python: "3.6"
install:
  - pip install -r requirements.txt
script:
  - cd tests && pytest && cd -
  - cd tests && ./tailtrim_test.sh && cd -
  - cd sample && python3 sample.py && cd -
